name = "zettel-web-ui"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# ── Secrets (set via CLI or Pages dashboard — never stored here) ──────────────
# wrangler pages secret put KINDE_CLIENT_ID             --project-name zettel-web-ui
# wrangler pages secret put KINDE_CLIENT_SECRET         --project-name zettel-web-ui
# wrangler pages secret put KINDE_ISSUER_URL            --project-name zettel-web-ui
# wrangler pages secret put KINDE_POST_LOGIN_REDIRECT_URL  --project-name zettel-web-ui
# wrangler pages secret put KINDE_POST_LOGOUT_REDIRECT_URL --project-name zettel-web-ui
# wrangler pages secret put BACKEND_URL                 --project-name zettel-web-ui
