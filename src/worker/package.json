{
  "name": "zettel-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "cd ../zettel-web-ui && npm ci && npm run build"
  },
  "dependencies": {
    "ai": "^6.0.138",
    "ai-gateway-provider": "^3.1.2",
    "drizzle-orm": "^0.43.1",
    "hono": "^4.12.9",
    "jose": "^5.10.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250404.0",
    "drizzle-kit": "^0.31.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.77.0"
  }
}
